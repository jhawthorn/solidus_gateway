sudo: required
dist: trusty
cache: bundler
language: ruby
rvm:
  - 2.3.1
before_install:
  - mysql -e 'SET PASSWORD FOR "root"@"localhost" = PASSWORD("");' -uroot
env:
  matrix:
    - SOLIDUS_BRANCH=master DB=mysql DATABASE_URL=mysql2://root:@localhost/solidus_gateway_solidus_test
